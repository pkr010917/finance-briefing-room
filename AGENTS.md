# 금융 브리핑 룸

매일 아침 텔레그램으로 받는 데일리 브리핑의 뉴스가, 웹사이트에서 트렌드 그래프로 쌓이는 구조.
목적은 두 가지 — **받아본 뉴스를 다시 떠올리기 쉽게 만들기**, 그리고 **트렌드 파악**.
사이트: https://pkr010917.github.io/finance-briefing-room/
(2026-07-21 telegram-finance-newsletter 저장소를 이곳으로 통합)

## 디자인 원칙 (2026-08-17 전면 개편)

**블룸버그 터미널 스타일(다크+앰버+모노스페이스)은 폐기됨. 되돌리지 말 것.**

조너선 아이브의 철학 — 본질만 남기고 나머지는 물러선다:

- **흰 배경.** 다크 테마 금지
- **색은 초록 하나뿐** (`--green`, 오늘 새로 연결된 뉴스). 색으로 장식하지 않는다
- 나머지는 무채색(잉크/회색)과 헤어라인. 테두리·그림자·둥근 모서리는 최소한으로
- 폰트는 Pretendard 하나. 굵기와 크기로만 위계를 만든다
- 그래프가 주인공 — UI 요소는 그래프를 방해하지 않는 선까지만 존재

## 화면 구조 (빌드 도구 없음 — 순수 HTML/CSS/JS)

옵시디언식 그래프. 트렌드(허브, 흰 원) 주위에 뉴스 기사(잎, 작은 점)가 선으로 연결됨.

- `index.html` — 상단(제목·핫 트렌드 칩) + 그래프 + 패널 + 하단
- `style.css` — 색 변수는 파일 상단 `:root`에 정의
- `script.js` — 외부 라이브러리 없는 자체 물리 시뮬레이션으로 배치
  - 허브 클릭 → 오른쪽 패널(고정 설명 + 연결된 뉴스 목록)
  - 잎 클릭 → 기사 원문 새 탭
  - 오늘 추가된 잎은 초록, 나머지는 회색
  - **지난 7일 뉴스가 가장 많은 트렌드 1개**에만 초록 배광 (핫 트렌드)
  - 좁은 화면(<720px)에서는 허브당 잎을 8개만 표시 — 다 찍으면 겹쳐서 오히려 안 보임.
    단 상단 집계는 화면이 아니라 **데이터 전체** 기준으로 낸다
  - 스타일 적용 전 SVG를 측정하면 그래프가 구석에 뭉개지므로 `whenStageSized()`로 대기

## 데이터

- `data/trends.json` — **단일 소스**
  - `trends[].desc` — **고정 설명. 자동으로 바뀌지 않으며 바꾸지 않는다.**
    카테고리는 대주제라 틀이 잘 안 바뀌므로, 시의성 표현 대신 구조적 배경·의미·관전
    포인트를 담아 길게 써두고 그대로 둔다. (매일 다시 쓰던 방식은 2026-08-17 폐기)
  - `trends[].articles` — 뉴스레터가 매일 축적 (URL 중복 제거, 트렌드당 최근 20건)
  - `unclassified` — 어느 트렌드에도 안 맞는 기사. 새 카테고리 발굴의 재료
- `data/history.json` — 뉴스레터가 최근 다룬 주제 (중복 방지용)

## 파이프라인

`.github/workflows/daily.yml` — 평일 KST 07:00

1. `scripts/rss_feeds.py` — RSS로 뉴스 수집 (**API 비용 0원**, 표준 라이브러리만)
2. `scripts/newsletter_bot.py` — RSS 기사로 뉴스레터 작성 → 텔레그램 발송 → 트렌드별 축적
3. `scripts/discover_trends.py` — **새 카테고리 발굴**. 아래 '발굴 기준' 참고
4. `data/` 커밋·푸시

> `scripts/update_briefing.py`(네이버 수집 + 설명 재작성)는 2026-08-17 삭제.
> 설명이 어차피 안 바뀌는데 매일 토큰을 썼기 때문. 그 예산은 발굴로 옮겼다.

## 모델·비용 (2026-08-17 하이브리드 전환)

**핵심: 대화에 쌓이는 것이 비용이다.** 도구가 가져온 내용은 이후 모든 턴에 재전송되므로,
검색·본문읽기 횟수가 토큰을 지배한다. RSS는 파이썬이 받아오므로 이 누적이 없다.

- 뉴스레터: `claude-sonnet-5`, 생각하기 끔(`thinking: disabled`)
  - Sonnet 5는 thinking을 생략하면 자동으로 켜져 MAX_TOKENS를 잡아먹으므로 **명시적으로 꺼야 함**
  - 1️⃣2️⃣ 섹션 = RSS 기사 (비용 0원). 프롬프트에 상위 70건, 약 8,000 토큰
  - 3️⃣ 채용 섹션 = `web_search` 2회 (RSS가 채용 공고를 못 잡아서 남겨둔 것)
  - `web_fetch` 2회 / 건당 2,500토큰 — **더 늘리지 말 것.** 3회×6,000토큰으로 뒀을 때
    RSS 절감분이 전부 상쇄돼 기존과 같은 $0.39가 됐다
  - ⚠️ `web_search_20260209`(자동 필터링)은 쓰지 말 것 — 내부 코드 실행이 max_uses를
    함께 소모해 모델이 한도에 걸려 헛돌고 토큰이 3배로 튐 (2026-08-16 사고)
- 발굴: `claude-sonnet-5` (변수 `DISCOVER_MODEL`로 교체 가능)
- 예상: 1회 $0.23~0.31 → **월 약 $5~7** (전환 전 $0.38 / 월 $8.3)

## RSS 피드 (scripts/rss_feeds.py)

2026-08-17 실측으로 살아있음을 확인한 것만 등록: 한국경제(금융·경제), 연합뉴스 경제,
연합인포맥스, 머니투데이. **매일경제는 403으로 봇을 차단**하고 이데일리·서울경제·
파이낸셜뉴스는 404(주소 폐기) — 10개 중 4개가 이미 죽어 있었다.

- `focused: False` 피드는 정치·연예가 섞여 오므로 `FINANCE_KEYWORDS`로 걸러낸다
- `EXCLUDE_PATTERNS`는 항상 적용 — "[부고]…(메리츠증권 사장)씨 빙부상"이 '증권' 때문에
  통과한 사례가 있었다
- **URL 중복 제거는 `dedupe_key()`로 한다.** 물음표 뒤를 통째로 자르면 안 된다 —
  연합인포맥스는 `?idxno=` 로 기사를 구분하므로 24건이 1건으로 뭉개졌다 (실제 발생)
- 피드는 조용히 죽는다. 수집이 10건 미만이면 `newsletter_bot.py`가 **중단**한다.
  검색으로 조용히 대체하면 커버리지가 준 걸 모른 채 지나가기 때문

## 새 트렌드 발굴 기준 (2026-08-18 대폭 강화 — 낮추지 말 것)

프롬프트만으로는 과잉 생성을 못 막았다. **하루에 5개가 추가돼 8개 → 13개**가 된 적이 있고,
그때 들어온 것은 폭우 대응(계절성), 종부세 특례(은행업과 무관), 공공기관 지방이전(행정 이슈),
"원화 강세 구조화: 수출기업 달러 매도에…"(문장형 제목)처럼 원래 8개와 성격이 달랐다.
그래서 프롬프트 + **코드 양쪽**에서 건다.

| 문턱 | 값 | 이유 |
|---|---|---|
| `MIN_UNCLASSIFIED` | 25 | 이만큼 안 쌓이면 API 호출조차 안 함 |
| `MIN_ARTICLES_PER_TREND` | 8 | 근거 3건짜리 트렌드가 만들어졌었다 |
| `MAX_NEW_PER_RUN` | 1 | 한 번에 5개가 들어온 재발 방지 |
| `MAX_TOTAL_TRENDS` | 12 | 넘으면 발굴 자체를 중단 |
| `MAX_TITLE_LENGTH` | 20 | 문장형 제목은 트렌드가 아니라 기사 제목 |
| `DISCOVERY_INTERVAL_DAYS` | 7 | 매일 시도하지 않음. `lastDiscoveryAt`로 추적 |

프롬프트에는 원래 8개를 **합격선 예시**로, 잘못 추가됐던 4개를 **불합격 예시**로 넣는다
(`REFERENCE_TRENDS`). 추상적 규칙보다 실제 사례가 훨씬 잘 먹힌다.
합격선 요약: **은행·금융지주가 어떻게 돈을 버는지에 직접 영향 + 1년 이상 지속 + 짧은 명사형 제목.**

## 안전장치 (지우지 말 것)

- `generate_newsletter`가 `stop_reason` 확인 — `pause_turn`은 최대 2회 이어붙여 재개,
  그 외 비정상 종료는 발송하지 않고 실패
- `validate_newsletter`가 발송 전 본문 길이·`<topics>` 존재·섹션 번호 점검 → 실패 시 빨간 X
- `strip_preamble`이 "리서치하겠습니다" 같은 진행 멘트를 제거 (Sonnet 5는 검색 중 중계함)

> 이 장치들이 없던 시절, 모델이 "다시 시도하겠습니다"만 반복한 응답이 그대로 발송되고
> 워크플로는 초록 체크로 끝난 적 있음 (2026-08-16).
> **API 잔액이 0이면 알림 없이 매일 실패한다** — 텔레그램이 안 오면 Billing부터 확인.

Secrets: `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET`(현재 미사용), `ANTHROPIC_API_KEY`,
`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`.

## 실행 방법

```
python3 -m http.server 8765
```

브라우저에서 http://localhost:8765 접속.

## 사용자 참고

- 사용자는 코딩 입문자 — 설명은 쉽게, 한국어로
- 터미널 git 인증이 안 되어 있어 **push는 GitHub Desktop**으로 함.
  내가 로컬 수정·commit까지, push는 사용자가 누르는 분업
- 봇이 매일 원격에 커밋하므로 로컬 수정 전 반드시 Fetch/Pull 먼저
