# 🤖 자동 브리핑 설정 가이드 (클릭 단위 상세판)

매일 아침 7시, AI가 뉴스를 읽고 사이트를 자동 갱신하게 만드는 설정입니다.
한 번만 하면 되고, 순서대로 따라 하면 30~40분 정도 걸립니다.

> 💡 막히면 그 단계 화면을 캡처해서 Claude에게 보여주세요. 어디를 눌러야 할지 알려드립니다.

---

## 전체 그림 (왜 이걸 하는지)

| 단계 | 하는 일 | 왜 필요한가 |
|---|---|---|
| 1 | GitHub에 코드 올리기 | 자동 실행(Actions)과 무료 호스팅(Pages)이 GitHub 안에서 돌아감 |
| 2 | 네이버 API 키 발급 | 스크립트가 네이버 뉴스를 검색할 때 쓰는 출입증 |
| 3 | Anthropic API 키 발급 | 스크립트가 Claude에게 글을 쓰게 할 때 쓰는 출입증 |
| 4 | 키 3개를 GitHub에 등록 | 자동 실행될 때 GitHub가 이 키들을 스크립트에 건네줌 |
| 5 | 테스트 실행 | 잘 되는지 확인 |
| 6 | GitHub Pages 켜기 | 사이트를 인터넷에 공개 |

---

## 1. GitHub에 프로젝트 올리기

### 1-1. 가입

1. 브라우저에서 `github.com` 접속
2. 오른쪽 위 **Sign up** 클릭
3. 이메일 입력 → **Continue** → 비밀번호, 아이디(username) 정하기
   - 아이디는 사이트 주소에 들어갑니다 (예: 아이디가 `park0917`이면 사이트 주소는 `park0917.github.io/...`)
4. 이메일로 온 인증 코드 입력하면 가입 완료

### 1-2. 저장소(repository) 만들기

> 저장소 = 프로젝트 하나를 담는 GitHub 위의 폴더

1. 로그인 상태에서 화면 **오른쪽 위의 `+` 아이콘** 클릭 → **New repository** 클릭
2. 입력 화면에서:
   - **Repository name** 칸: `finance-briefing-room` 입력
   - **Description** 칸: 비워도 됨 (원하면 "금융 트렌드 브리핑" 등)
   - **Public / Private** 선택지: **Public** 선택 ← 중요! (무료 Pages는 Public만 가능)
   - 아래 체크박스들 (**Add a README file** 등): **전부 체크하지 않은 채로** 둠
     (이미 내 컴퓨터에 코드가 있어서, 빈 저장소가 필요합니다)
3. 초록색 **Create repository** 버튼 클릭
4. "Quick setup" 이라고 적힌 파란 상자가 있는 페이지가 나오면 성공.
   그 페이지의 `https://github.com/아이디/finance-briefing-room.git` 주소를 복사해두세요
   (주소 오른쪽의 📋 복사 버튼 클릭)

### 1-3. 내 컴퓨터의 코드를 저장소로 올리기 (터미널)

1. 맥에서 `Cmd + Space` → "터미널" 입력 → Enter
2. 아래 세 줄을 **한 줄씩** 복사해 붙여넣고, 줄마다 Enter:

```bash
cd "/Users/park/vibe-coding-projects/finanace-breifing room"
```
> "이 프로젝트 폴더로 이동해줘" (cd = change directory)

```bash
git remote add origin https://github.com/아이디/finance-briefing-room.git
```
> "이 폴더를 아까 만든 GitHub 저장소와 연결해줘"
> ⚠️ `아이디` 부분을 본인 GitHub 아이디로 바꿔서 입력! (1-2에서 복사한 주소를 그대로 붙여도 됨)

```bash
git push -u origin main
```
> "연결된 저장소로 코드를 업로드해줘" (push = 밀어올리기)

3. 처음 push하면 **로그인 창이 뜹니다**:
   - "Sign in with your browser" 같은 버튼이 보이면 클릭 → 브라우저에서 **Authorize** 클릭
   - 터미널에 아이디/비밀번호를 물어보면: 비밀번호 대신 토큰이 필요해서 복잡해집니다.
     이 경우 그냥 Claude에게 "push에서 막혔어"라고 알려주세요.
4. 성공하면 터미널에 `main -> main` 같은 글자가 나옵니다.
   브라우저에서 저장소 페이지를 새로고침 → 파일 목록(index.html, data.json 등)이 보이면 완료! 🎉

---

## 2. 네이버 API 키 발급 (무료)

1. `developers.naver.com` 접속 → 오른쪽 위 **로그인** (네이버 계정)
2. 위쪽 메뉴 **Application → 애플리케이션 등록** 클릭
   (약관 동의/휴대폰 인증을 요구하면 진행)
3. 등록 화면에서:
   - **애플리케이션 이름**: `금융브리핑룸` (아무거나 OK)
   - **사용 API**: 목록에서 **검색** 선택 (+ 버튼)
   - **비로그인 오픈 API 서비스 환경**: **WEB 설정** 선택
   - **웹 서비스 URL** 칸: `http://localhost` 입력
4. **등록하기** 클릭
5. 나오는 화면에서 두 값을 메모장에 복사:
   - **Client ID** (영문+숫자)
   - **Client Secret** (옆의 "보기" 클릭하면 표시)

---

## 3. Anthropic API 키 발급

1. `console.anthropic.com` 접속 → **Sign up** (구글 계정으로 가입 가능)
2. 왼쪽 메뉴(또는 설정)에서 **Billing** 클릭 → **Add credits** 또는 카드 등록 → **$5** 충전
   (선불 충전 방식이라 이 이상 빠져나갈 걱정은 없습니다)
3. 왼쪽 메뉴 **API Keys** 클릭 → **Create Key** 버튼
   - 이름: `briefing` (아무거나 OK) → 생성
4. `sk-ant-`로 시작하는 긴 키가 나오면 **바로 복사**해서 메모장에 보관
   ⚠️ 이 키는 창을 닫으면 다시 볼 수 없습니다. 잃어버리면 새로 만들면 되긴 합니다.
   ⚠️ 이 키는 비밀번호와 같습니다. 남에게 보여주거나 코드 파일 안에 적지 마세요.

---

## 4. 키 3개를 GitHub에 등록

> 왜? 자동 실행될 때 GitHub가 이 "비밀금고"에서 키를 꺼내 스크립트에 넣어줍니다.
> 코드에 키를 직접 적으면 전세계에 공개되니까 절대 그렇게 하지 않습니다.

1. 브라우저에서 내 저장소 페이지 접속 (`github.com/아이디/finance-briefing-room`)
2. 저장소 위쪽 탭 중 **⚙️ Settings** 클릭 (저장소 안의 Settings — 프로필 설정 아님)
3. 왼쪽 사이드바에서 **Secrets and variables** 클릭 → 펼쳐지면 **Actions** 클릭
4. 초록색 **New repository secret** 버튼 클릭
5. 첫 번째 키 등록:
   - **Name** 칸: `NAVER_CLIENT_ID` (대문자·언더바까지 정확히 이대로)
   - **Secret** 칸: 2단계에서 메모한 네이버 Client ID 붙여넣기
   - **Add secret** 클릭
6. 같은 방법으로 두 개 더:

| Name (정확히 이대로) | Secret 칸에 넣을 값 |
|---|---|
| `NAVER_CLIENT_SECRET` | 네이버 Client Secret |
| `ANTHROPIC_API_KEY` | Anthropic 키 (sk-ant-...) |

7. 목록에 세 줄이 보이면 완료 (값은 등록 후 다시 볼 수 없는 게 정상입니다)

---

## 5. 테스트 실행

1. 저장소 페이지 위쪽 탭에서 **Actions** 클릭
   - "Workflows aren't being run on this repository" 같은 안내가 뜨면
     초록 버튼(**I understand..., enable them**)을 눌러 활성화
2. 왼쪽 목록에서 **daily-briefing** 클릭
3. 오른쪽에 파란 안내줄과 함께 **Run workflow ▾** 버튼이 보임 → 클릭 → 초록 **Run workflow** 클릭
4. 몇 초 뒤 새로고침하면 노란 점(실행 중)이 생김 → 1~3분 기다림
5. 결과 확인:
   - ✅ **초록 체크** = 성공! 저장소의 `data.json`을 열어보면 lastUpdated가 오늘 날짜로 바뀌어 있음
   - ❌ **빨간 X** = 실패. 클릭해서 → **update** 클릭 → 빨간 단계를 클릭하면 에러 메시지가 보임
     - `환경변수 ... 없습니다` → 4단계에서 Name 오타 확인
     - `401` / `authentication` → 키 값을 잘못 붙여넣음 (다시 등록)
     - 그 외는 에러 메시지를 복사해서 Claude에게 보여주세요

---

## 6. 사이트를 인터넷에 공개 (GitHub Pages)

1. 저장소 **Settings** 탭 → 왼쪽 사이드바에서 **Pages** 클릭
2. **Build and deployment** 아래:
   - **Source**: `Deploy from a branch` 선택 (기본값)
   - **Branch**: 드롭다운에서 `main` 선택, 옆 폴더는 `/ (root)` 그대로 → **Save**
3. 1~2분 뒤 페이지를 새로고침하면 위에
   "Your site is live at `https://아이디.github.io/finance-briefing-room/`" 라고 뜸
4. 그 주소로 접속 → 브리핑 룸이 보이면 전부 완성입니다! 🎉
   즐겨찾기 해두고 매일 아침 확인하세요. 7시에 자동으로 새 브리핑이 반영됩니다.

---

## 💰 비용 정리

- 네이버 API, GitHub Actions, GitHub Pages: **전부 무료**
- Claude API: 기본 모델(claude-opus-4-8) 기준 **1회 약 300~500원**, 매일 실행 시 월 1만 원 안팎
- **더 저렴하게**: Settings → Secrets and variables → Actions → **Variables 탭** →
  **New repository variable** → Name: `BRIEFING_MODEL`, Value: `claude-haiku-4-5`
  → 1회 약 50~100원, 월 2~3천 원 (품질은 조금 낮아지지만 이 용도엔 충분한 편)

## 🔧 자주 묻는 것

- **실행 시간 바꾸기** → `.github/workflows/daily-briefing.yml`의 `cron: "0 22 * * *"` 수정
  (UTC 기준이라 한국시간 −9시간. 예: 아침 8시 = `"0 23 * * *"`)
- **트렌드 추가/삭제** → `data.json` 수정 (`query`가 뉴스 검색어)
- **내 컴퓨터에서 미리 테스트** →
  ```bash
  pip3 install anthropic
  NAVER_CLIENT_ID=아이디 NAVER_CLIENT_SECRET=시크릿 ANTHROPIC_API_KEY=키 \
    python3 scripts/update_briefing.py
  ```

## 📚 방금 배운 GitHub 용어 정리

| 용어 | 뜻 |
|---|---|
| repository (저장소) | 프로젝트 하나를 담는 GitHub 위의 폴더 |
| push | 내 컴퓨터 → GitHub로 코드 업로드 |
| commit | 변경사항을 하나의 기록으로 저장한 것 (저장 스냅샷) |
| Actions | GitHub가 대신 프로그램을 돌려주는 로봇 (우리는 매일 7시 예약) |
| Secrets | API 키 같은 비밀값을 안전하게 보관하는 금고 |
| Pages | 저장소의 HTML을 무료로 인터넷에 공개해주는 기능 |
