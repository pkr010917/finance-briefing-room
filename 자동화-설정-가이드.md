# 🤖 자동 브리핑 설정 가이드 (딱 한 번만 하면 됩니다)

매일 아침 7시, AI가 뉴스를 읽고 사이트를 자동 갱신하게 만드는 설정입니다.
아래 순서대로 따라 하면 약 20~30분 걸립니다.

---

## 준비물 3가지 (전부 무료 가입)

| 준비물 | 어디서 | 비용 |
|---|---|---|
| GitHub 계정 | github.com | 무료 |
| 네이버 API 키 | developers.naver.com | 무료 (하루 25,000회) |
| Anthropic API 키 | console.anthropic.com | 크레딧 충전 필요 (최소 $5) |

---

## 1. GitHub에 프로젝트 올리기

1. [github.com](https://github.com) 가입 후 우측 상단 **+ → New repository**
2. 이름: `finance-briefing-room` / **Public** 선택 / **Create repository**
3. 터미널에서 (이미 git 준비는 되어 있음):

```bash
cd "/Users/park/vibe-coding-projects/finanace-breifing room"
git remote add origin https://github.com/내아이디/finance-briefing-room.git
git push -u origin main
```

> `내아이디` 부분만 본인 GitHub 아이디로 바꾸세요.

## 2. 네이버 API 키 발급

1. [developers.naver.com](https://developers.naver.com) → 로그인 → **Application → 애플리케이션 등록**
2. 애플리케이션 이름: 아무거나 (예: 금융브리핑룸)
3. 사용 API: **검색** 선택
4. 환경: **WEB** 선택, URL은 `http://localhost` 입력
5. 등록하면 나오는 **Client ID**와 **Client Secret**을 메모

## 3. Anthropic API 키 발급

1. [console.anthropic.com](https://console.anthropic.com) 가입
2. **Billing**에서 크레딧 충전 (최소 $5 — 몇 달은 쓸 수 있는 양)
3. **API Keys → Create Key** → 나온 키(`sk-ant-...`)를 메모
   ⚠️ 키는 한 번만 보여주니 바로 복사해두세요

## 4. GitHub에 키 3개 등록 (비밀금고)

내 저장소 페이지에서: **Settings → Secrets and variables → Actions → New repository secret**

아래 3개를 하나씩 등록:

| Name (정확히 이대로) | Secret (값) |
|---|---|
| `NAVER_CLIENT_ID` | 네이버 Client ID |
| `NAVER_CLIENT_SECRET` | 네이버 Client Secret |
| `ANTHROPIC_API_KEY` | Anthropic 키 (sk-ant-...) |

## 5. 테스트 실행

1. 저장소의 **Actions** 탭 → 왼쪽 **daily-briefing** 클릭
2. 오른쪽 **Run workflow** 버튼 → 초록색으로 실행
3. ✅ 초록 체크가 뜨면 성공! `data.json`이 자동으로 갱신됩니다
4. ❌ 빨간 X가 뜨면 클릭해서 에러 메시지를 확인 (대부분 키 오타)

## 6. 사이트를 인터넷에 공개 (GitHub Pages)

1. 저장소 **Settings → Pages**
2. Source: **Deploy from a branch** / Branch: **main** / 폴더: **/ (root)** → Save
3. 1~2분 뒤 `https://내아이디.github.io/finance-briefing-room` 에서 사이트 접속 가능
4. 이제 매일 아침 7시에 자동으로 새 브리핑이 반영됩니다 🎉

---

## 💰 비용은?

- 네이버 API, GitHub Actions, GitHub Pages: **전부 무료**
- Claude API: 기본 모델(claude-opus-4-8) 기준 **1회 약 300~500원**, 매일 실행 시 월 1만 원 안팎
- 더 저렴하게: 저장소 **Settings → Secrets and variables → Actions → Variables 탭**에서
  `BRIEFING_MODEL` = `claude-haiku-4-5` 등록 → **1회 약 50~100원**, 월 2~3천 원 수준
  (품질은 조금 낮아지지만 이 용도엔 충분한 편)

## 🔧 자주 묻는 것

- **실행 시간을 바꾸고 싶어요** → `.github/workflows/daily-briefing.yml`의 `cron: "0 22 * * *"` 수정
  (UTC 기준이라 한국시간 −9시간. 예: 아침 8시 = `"0 23 * * *"`)
- **트렌드를 추가/삭제하고 싶어요** → `data.json`에서 직접 수정 (query가 뉴스 검색어)
- **내 컴퓨터에서 미리 테스트** →
  ```bash
  pip3 install anthropic
  NAVER_CLIENT_ID=아이디 NAVER_CLIENT_SECRET=시크릿 ANTHROPIC_API_KEY=키 \
    python3 scripts/update_briefing.py
  ```
